import { URL } from "node:url";
export type Severity="high"|"medium"|"low"; export type Evidence={label:string;detail:string;marker:string;x:number;y:number;width:number;height:number}; export type Finding={id:string;severity:Severity;category:string;title:string;description:string;recommendation:string;screenrootTasks:string[];devTasks:string[];uxPerspective:{law:string;definition:string;assessment:string};evidence:Evidence[]}; export type AuditPage={url:string;title:string;screenshot:string;findings:Finding[]}; export type AuditResult={pages:AuditPage[]};
const GEMINI_MODEL="gemini-3.8-flash",PAGE_FETCH_TIMEOUT_MS=10000,BROWSERLESS_TIMEOUT_MS=45000,GEMINI_TIMEOUT_MS=30000;
function normalizeFinding(value:unknown,index:number):Finding{const item=(value&&typeof value==="object"?value:{}) as Record<string,unknown>;const perspective=(item.uxPerspective&&typeof item.uxPerspective==="object"?item.uxPerspective:{}) as Record<string,unknown>;const evidence=Array.isArray(item.evidence)?item.evidence:[];return{id:typeof item.id==="string"?item.id:`finding-${index+1}`,severity:item.severity==="high"||item.severity==="low"?item.severity:"medium",category:typeof item.category==="string"?item.category:"UX",title:typeof item.title==="string"?item.title:"UX issue",description:typeof item.description==="string"?item.description:"The visual design may create friction for users.",recommendation:typeof item.recommendation==="string"?item.recommendation:"Review this area against established UX principles.",screenrootTasks:Array.isArray(item.screenrootTasks)?item.screenrootTasks.filter((task):task is string=>typeof task==="string"):[],devTasks:Array.isArray(item.devTasks)?item.devTasks.filter((task):task is string=>typeof task==="string"):[],uxPerspective:{law:typeof perspective.law==="string"?perspective.law:"UX principle",definition:typeof perspective.definition==="string"?perspective.definition:"A usability principle used to evaluate interface design.",assessment:typeof perspective.assessment==="string"?perspective.assessment:"This area deserves review based on the visible interface."},evidence:evidence.map((raw,evidenceIndex)=>{const entry=(raw&&typeof raw==="object"?raw:{}) as Record<string,unknown>;const numeric=(key:string,fallback:number)=>{const value=Number(entry[key]);return Number.isFinite(value)?Math.max(0,Math.min(100,value)):fallback};return{label:typeof entry.label==="string"?entry.label:`Region ${evidenceIndex+1}`,detail:typeof entry.detail==="string"?entry.detail:"Visual evidence identified in the screenshot.",marker:typeof entry.marker==="string"?entry.marker:`${index+1}`,x:numeric("x",10),y:numeric("y",10),width:numeric("width",20),height:numeric("height",10)}})}}
async function fetchPage(url:string){const response=await fetch(url,{redirect:"follow",signal:AbortSignal.timeout(PAGE_FETCH_TIMEOUT_MS),headers:{"user-agent":"UXAudit/1.0"}});if(!response.ok)throw new Error(`Page returned HTTP ${response.status}.`);const html=await response.text();const title=html.match(/<title[^>]*>([\s\S]*?)<\/title>/i)?.[1]?.replace(/\s+/g," ").trim()||new URL(url).hostname;return{title}}
async function captureScreenshot(url:string){const token=process.env.BROWSERLESS_API_TOKEN;if(!token)throw new Error("BROWSERLESS_API_TOKEN is not configured on this deployment.");const response=await fetch(`https://production-sfo.browserless.io/screenshot?token=${encodeURIComponent(token)}`,{method:"POST",headers:{"Content-Type":"application/json","Cache-Control":"no-cache"},body:JSON.stringify({url,options:{fullPage:true,type:"png",optimizeForSpeed:true}}),signal:AbortSignal.timeout(BROWSERLESS_TIMEOUT_MS)});if(!response.ok)throw new Error(`Browserless returned HTTP ${response.status}.`);return Buffer.from(await response.arrayBuffer())}
async function analyseWithGemini(url:string,title:string,screenshot:Buffer):Promise<Finding[]>{const apiKey=process.env.GEMINI_API_KEY;if(!apiKey)throw new Error("GEMINI_API_KEY is not configured on this deployment.");const prompt=`You are a senior UX auditor for ScreenRoot. Analyze ONLY the supplied landing-page screenshot for ${url} (title: ${title}). Do not infer hidden interactions or inspect source code. Identify 3 to 8 meaningful, visually evidenced UX issues. Prioritize issues that materially affect clarity, hierarchy, discoverability, trust, accessibility, conversion, or cognitive load. For every finding return severity (high|medium|low), category, title, description, recommendation, screenrootTasks (array), devTasks (array), uxPerspective with law, definition, assessment, and evidence (array). Every evidence item must identify one visible region and provide x, y, width, height as percentages of the full screenshot from 0 to 100, plus label, detail, and a short numeric marker matching the finding number. Keep boxes tightly around the visible evidence. Use recognized UX laws/principles where genuinely applicable. Do not force a law when it does not fit. Return ONLY valid JSON in this shape: {"findings":[{"id":"finding-1","severity":"high","category":"...","title":"...","description":"...","recommendation":"...","screenrootTasks":["..."],"devTasks":["..."],"uxPerspective":{"law":"...","definition":"...","assessment":"..."},"evidence":[{"label":"Region 1","detail":"...","marker":"1","x":0,"y":0,"width":10,"height":10}]}]}.`;const response=await fetch(`https://generativelanguage.googleapis.com/v1beta/models/${GEMINI_MODEL}:generateContent`,{method:"POST",headers:{"Content-Type":"application/json","X-goog-api-key":apiKey},body:JSON.stringify({contents:[{role:"user",parts:[{text:prompt},{inlineData:{mimeType:"image/png",data:screenshot.toString("base64")}}]}],generationConfig:{responseMimeType:"application/json",maxOutputTokens:6000}}),signal:AbortSignal.timeout(GEMINI_TIMEOUT_MS)});const detail=await response.text();if(!response.ok){let message=`Gemini returned HTTP ${response.status}.`;try{message=JSON.parse(detail)?.error?.message||message}catch{}throw new Error(message)}const parsed=JSON.parse(detail);const text=parsed?.candidates?.[0]?.content?.parts?.map((part:any)=>part?.text||"").join("")||"";const json=JSON.parse(text);return(Array.isArray(json?.findings)?json.findings:[]).map(normalizeFinding)}
export async function createAudit(url:string):Promise<AuditResult>{const page=await fetchPage(url);const screenshot=await captureScreenshot(url);let findings:Finding[];try{findings=await analyseWithGemini(url,page.title,screenshot)}catch(error){console.error("Gemini analysis failed",error);findings=[normalizeFinding({id:"ai-unavailable",severity:"medium",category:"AI analysis",title:"AI analysis unavailable",description:error instanceof Error?error.message:"Gemini analysis could not be completed.",recommendation:"Check the Gemini configuration and retry the audit.",screenrootTasks:[],devTasks:[],uxPerspective:{law:"N/A",definition:"No UX law assessment was generated.",assessment:"Gemini did not return a usable analysis."},evidence:[]},0)]}return{pages:[{url,title:page.title,screenshot:`data:image/png;base64,${screenshot.toString("base64")}`,findings}]}}
