import { NextResponse } from "next/server";
export const runtime="nodejs"; const GEMINI_MODEL="gemini-3.8-flash";
export async function GET(){const apiKey=process.env.GEMINI_API_KEY;if(!apiKey)return NextResponse.json({ok:false,configured:false,model:GEMINI_MODEL,error:"GEMINI_API_KEY is not configured on this deployment."},{status:503});try{const response=await fetch(`https://generativelanguage.googleapis.com/v1beta/models/${GEMINI_MODEL}:generateContent`,{method:"POST",headers:{"Content-Type":"application/json","X-goog-api-key":apiKey},body:JSON.stringify({contents:[{role:"user",parts:[{text:"Reply with exactly: GEMINI_OK"}]}],generationConfig:{thinkingConfig:{thinkingLevel:"low"},maxOutputTokens:100}}),signal:AbortSignal.timeout(15000)});const detail=await response.text();let parsed:any=null;try{parsed=JSON.parse(detail)}catch{}if(!response.ok)return NextResponse.json({ok:false,configured:true,model:GEMINI_MODEL,status:response.status,error:parsed?.error?.message||`Gemini returned HTTP ${response.status}.`},{status:502});const candidate=parsed?.candidates?.[0];const text=(candidate?.content?.parts??[]).map((part:any)=>typeof part?.text==="string"?part.text:"").join("");return NextResponse.json({ok:text.trim()==="GEMINI_OK",configured:true,model:GEMINI_MODEL,response:text,modelVersion:parsed?.modelVersion??null,responseId:parsed?.responseId??null,finishReason:candidate?.finishReason??null,candidateCount:Array.isArray(parsed?.candidates)?parsed.candidates.length:0});}catch(error){return NextResponse.json({ok:false,configured:true,model:GEMINI_MODEL,error:error instanceof Error?error.message:"Gemini connection failed."},{status:502})}}
