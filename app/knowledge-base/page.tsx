"use client";

import { useEffect, useMemo, useState } from "react";
import type { Finding } from "@/lib/audit";

type Client = { id: string; name: string; created_at: string; updated_at: string };
type Audit = { id: string; client_id: string; client_name: string; title: string; source_filename: string; created_at: string; screenshot_width: number; screenshot_height: number; view_mode?: string; report_generated_at?: string | null };
type StoredFinding = { id: string; finding_index: number; finding: Finding; created_at: string };
type AuditDetail = Audit & { screenshot: string; report_html?: string | null; findings: StoredFinding[] };

function formatDate(value: string) { return new Intl.DateTimeFormat("en-IN", { dateStyle: "medium", timeStyle: "short" }).format(new Date(value)); }
function viewLabel(value?: string) { return value === "mobile" ? "Mobile responsive" : value === "tablet" ? "Tablet" : value === "desktop" ? "Desktop web" : "View mode unknown"; }
const markerColors = ["pink", "purple", "orange", "blue", "green", "yellow", "violet", "teal", "rose", "cyan"];
function markerColor(index: number) { return markerColors[index % markerColors.length]; }

export default function KnowledgeBasePage() {
  const [clients, setClients] = useState<Client[]>([]); const [audits, setAudits] = useState<Audit[]>([]); const [audit, setAudit] = useState<AuditDetail | null>(null); const [selectedFinding, setSelectedFinding] = useState(0); const [loading, setLoading] = useState(true); const [error, setError] = useState(""); const [selectedClient, setSelectedClient] = useState<Client | null>(null); const [generating, setGenerating] = useState(false);
  async function loadClients() { setLoading(true); setError(""); setSelectedClient(null); setAudits([]); setAudit(null); try { const response = await fetch("/api/knowledge-base", { cache: "no-store" }); const data = await response.json(); if (!response.ok) throw new Error(data.error || "Could not load clients."); setClients(data.clients || []); } catch (err) { setError(err instanceof Error ? err.message : "Could not load the knowledge base."); } finally { setLoading(false); } }
  async function openClient(client: Client) { setSelectedClient(client); setAudit(null); setSelectedFinding(0); setLoading(true); setError(""); try { const response = await fetch(`/api/knowledge-base?clientId=${encodeURIComponent(client.id)}`, { cache: "no-store" }); const data = await response.json(); if (!response.ok) throw new Error(data.error || "Could not load audits."); setAudits(data.audits || []); } catch (err) { setError(err instanceof Error ? err.message : "Could not load audits."); } finally { setLoading(false); } }
  async function openAudit(item: Audit) { setAudit(null); setSelectedFinding(0); setLoading(true); setError(""); try { const response = await fetch(`/api/knowledge-base?auditId=${encodeURIComponent(item.id)}`, { cache: "no-store" }); const data = await response.json(); if (!response.ok || !data.audit) throw new Error(data.error || "Could not load the analysis."); setAudit(data.audit); } catch (err) { setError(err instanceof Error ? err.message : "Could not load the analysis."); } finally { setLoading(false); } }
  async function generateReport() { if (!audit) return; setGenerating(true); setError(""); try { const response = await fetch("/api/report", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ auditId: audit.id }) }); const data = await response.json(); if (!response.ok) throw new Error(data.error || "Could not generate the report."); setAudit((previous) => previous ? { ...previous, report_generated_at: data.generatedAt } : previous); window.open(data.reportUrl, "_blank", "noopener,noreferrer"); } catch (err) { setError(err instanceof Error ? err.message : "Could not generate the report."); } finally { setGenerating(false); } }
  useEffect(() => { void loadClients(); }, []);
  const finding = audit?.findings[selectedFinding]?.finding; const highCount = useMemo(() => audit?.findings.filter((item) => item.finding.severity === "high").length || 0, [audit]);

  return <main className="kbShell">
    <header className="kbNav"><a href="/" className="kbBrand">UX Audit <span>by ScreenRoot</span></a><a href="/" className="kbBack">← New audit</a></header>
    <section className="kbHero"><div><div className="kbEyebrow">Knowledge base</div><h1>Client findings, kept in one place.</h1><p>Every completed audit is stored against the detected client name. Browse previous analyses by client, audit date and individual finding.</p></div><div className="kbStat"><strong>{clients.length}</strong><span>clients</span></div></section>
    {error && <div className="kbError">{error}</div>}{loading && <div className="kbLoading">Loading knowledge base…</div>}
    {!loading && !selectedClient && <section className="kbContent"><div className="kbSectionHead"><div><span>Clients</span><h2>Choose a client</h2></div><span>{clients.length} total</span></div><div className="clientGrid">{clients.map((client) => <button className="clientCard" key={client.id} onClick={() => void openClient(client)}><div className="clientInitial">{client.name.slice(0, 1).toUpperCase()}</div><div><h3>{client.name}</h3><p>View audit history →</p></div></button>)}{!clients.length && <div className="kbEmpty">No audits have been stored yet. Run your first screenshot audit and it will appear here.</div>}</div></section>}
    {!loading && selectedClient && !audit && <section className="kbContent"><button className="crumbBack" onClick={() => void loadClients()}>← All clients</button><div className="kbSectionHead"><div><span>Client</span><h2>{selectedClient.name}</h2></div><span>{audits.length} audits</span></div><div className="auditList">{audits.map((item) => <div className="auditRow" key={item.id}><button className="auditMainButton" onClick={() => void openAudit(item)}><div className="auditDate"><strong>{new Intl.DateTimeFormat("en-IN", { day: "2-digit", month: "short" }).format(new Date(item.created_at))}</strong><span>{new Intl.DateTimeFormat("en-IN", { year: "numeric" }).format(new Date(item.created_at))}</span></div><div><h3>{item.title}</h3><p>{formatDate(item.created_at)} · {item.screenshot_width} × {item.screenshot_height}px · {viewLabel(item.view_mode)}</p></div></button><div className="auditActions">{item.report_generated_at ? <a href={`/report/${encodeURIComponent(item.id)}`} target="_blank" rel="noreferrer">View report ↗</a> : <button onClick={() => void openAudit(item)}>Open analysis →</button>}</div></div>)}{!audits.length && <div className="kbEmpty">No audits found for this client.</div>}</div></section>}
    {!loading && audit && <section className="kbContent"><button className="crumbBack" onClick={() => { setAudit(null); setSelectedFinding(0); }}>← {selectedClient?.name || "Client history"}</button><div className="analysisHeader"><div><span>Audit analysis</span><h2>{audit.client_name}</h2><p>{formatDate(audit.created_at)} · {audit.source_filename} · {viewLabel(audit.view_mode)}</p></div><div className="analysisActions"><button className="reportButton" onClick={() => audit.report_generated_at ? window.open(`/report/${encodeURIComponent(audit.id)}`, "_blank", "noopener,noreferrer") : void generateReport()} disabled={generating}>{generating ? "Generating report…" : audit.report_generated_at ? "View client report ↗" : "Generate client report →"}</button><div className="analysisStats"><strong>{audit.findings.length}</strong><span>findings</span><em>{highCount} high priority</em></div></div></div><div className="analysisLayout"><aside className="findingIndex"><div className="indexTitle">Findings</div>{audit.findings.map((item, index) => <button key={item.id} className={selectedFinding === index ? "active" : ""} onClick={() => setSelectedFinding(index)}><span className={`indexDot ${markerColor(index)}`}>{index + 1}</span><span><strong>{item.finding.title}</strong><small>{item.finding.category} · {item.finding.severity}</small></span></button>)}</aside><div className="analysisDetail">{finding && <><div className="detailTop"><span className={`severityBadge ${finding.severity}`}>{finding.severity}</span><span>{finding.category}</span></div><h2>{finding.title}</h2><p className="detailDescription">{finding.description}</p><div className="storedEvidence"><div className={`indexDot ${markerColor(selectedFinding)}`}>{selectedFinding + 1}</div><div><strong>{finding.evidence[0]?.element || "Analysed region"}</strong><span>{finding.evidence[0]?.section || "Page section"} · {finding.evidence[0]?.detail || "Visible evidence from the original screenshot."}</span></div></div><div className="detailGrid"><section><span>UX principle</span><h3>{finding.uxPerspective.law}</h3><p><b>Definition:</b> {finding.uxPerspective.definition}</p><p><b>Why it applies:</b> {finding.uxPerspective.assessment}</p><p><b>Research context:</b> {finding.uxPerspective.researchContext}</p></section><section><span>Business analysis</span><h3>{finding.businessAnalysis.funnelStage}</h3><p><b>Impact:</b> {finding.businessAnalysis.impact}</p><p><b>KPI at risk:</b> {finding.businessAnalysis.kpi}</p><p><b>Mechanism:</b> {finding.businessAnalysis.mechanism}</p><ul>{finding.businessAnalysis.suggestions.map((suggestion) => <li key={suggestion}>{suggestion}</li>)}</ul></section></div><div className="detailRecommendation"><b>Recommended redesign:</b> {finding.recommendation}</div><div className="storedScreenshot"><div className="storedScreenshotHead"><div><span>Evidence map</span><h3>Original analysed screenshot</h3></div><small>Finding {selectedFinding + 1} of {audit.findings.length}</small></div><div className="storedCanvas"><img src={audit.screenshot} alt={`Original ${audit.client_name} screenshot`} />{audit.findings.map((item, index) => { const marker = item.finding.evidence[0]?.marker; if (!marker) return null; return <button key={item.id} className={`storedMarker ${markerColor(index)} ${selectedFinding === index ? "selected" : ""}`} style={{ left: `${marker.x * 100}%`, top: `${marker.y * 100}%` }} onClick={() => setSelectedFinding(index)}>{index + 1}</button>; })}</div></div></>}</div></div></section>}
    <style jsx>{`
      .kbShell{min-height:100vh;background:#f7f6f1;color:#171717}.kbNav{height:76px;border-bottom:1px solid #deddd5;display:flex;align-items:center;justify-content:space-between;padding:0 48px;background:rgba(247,246,241,.96);position:sticky;top:0;z-index:20}.kbBrand{color:#111;text-decoration:none;font-size:21px;font-weight:800;letter-spacing:-.045em}.kbBrand span{font-size:10px;color:#77766e;letter-spacing:.08em;text-transform:uppercase;margin-left:9px}.kbBack{color:#55534c;text-decoration:none;font-size:12px;font-weight:750}.kbHero{max-width:1180px;margin:0 auto;padding:70px 24px 48px;display:flex;justify-content:space-between;gap:40px}.kbEyebrow{display:inline-flex;background:#f5d547;padding:7px 10px;border-radius:999px;font-size:10px;font-weight:850;letter-spacing:.08em;text-transform:uppercase}.kbHero h1{font-size:clamp(42px,6vw,68px);line-height:.96;letter-spacing:-.06em;max-width:760px;margin:18px 0}.kbHero p{max-width:690px;color:#686860;font-size:17px;line-height:1.55;margin:0}.kbStat{align-self:flex-end;border-left:1px solid #deddd5;padding-left:30px;min-width:120px}.kbStat strong{display:block;font-size:54px;line-height:1;letter-spacing:-.06em}.kbStat span{font-size:11px;color:#686860}.kbContent{max-width:1180px;margin:0 auto;padding:0 24px 90px}.kbSectionHead{display:flex;align-items:flex-end;justify-content:space-between;border-bottom:1px solid #deddd5;padding-bottom:13px;margin-bottom:14px}.kbSectionHead>div span,.analysisHeader>div>span,.storedScreenshotHead span,.detailGrid section>span{font-size:10px;font-weight:850;letter-spacing:.08em;text-transform:uppercase;color:#77766e}.kbSectionHead h2{margin:5px 0 0;font-size:28px;letter-spacing:-.04em}.kbSectionHead>span{font-size:11px;color:#77766e}.clientGrid{display:grid;grid-template-columns:repeat(3,1fr);gap:12px}.clientCard{border:1px solid #deddd5;background:#fff;border-radius:15px;padding:20px;display:flex;gap:13px;text-align:left;align-items:center;transition:.15s}.clientCard:hover{border-color:#111;transform:translateY(-2px);box-shadow:0 9px 24px rgba(17,17,17,.07)}.clientInitial{width:42px;height:42px;border-radius:50%;display:grid;place-items:center;background:#111;color:#fff;font-size:16px;font-weight:850;flex:0 0 auto}.clientCard h3{font-size:16px;margin:0 0 5px}.clientCard p{font-size:11px;color:#77766e;margin:0}.kbEmpty,.kbLoading,.kbError{border:1px solid #deddd5;background:#fff;border-radius:14px;padding:20px;color:#686860;font-size:13px}.kbError{max-width:1132px;margin:0 auto 20px;border-color:#e4b7b0;color:#963f35}.kbLoading{max-width:1132px;margin:0 auto 20px}.crumbBack{border:0;background:none;padding:0;margin-bottom:18px;color:#66645d;font-size:12px;font-weight:750}.auditList{display:grid;gap:8px}.auditRow{width:100%;display:grid;grid-template-columns:1fr auto;align-items:center;gap:16px;text-align:left;border:1px solid #deddd5;background:#fff;border-radius:14px;padding:12px 14px;transition:.15s}.auditRow:hover{border-color:#111;box-shadow:0 8px 20px rgba(17,17,17,.05)}.auditMainButton{border:0;background:none;padding:3px 4px;display:flex;gap:16px;align-items:center;text-align:left}.auditDate{min-width:78px;border-right:1px solid #e7e5dd;padding-right:12px}.auditDate strong{display:block;font-size:18px}.auditDate span{font-size:10px;color:#77766e}.auditMainButton h3{margin:0 0 4px;font-size:14px}.auditMainButton p{margin:0;color:#77766e;font-size:11px}.auditActions{display:flex;gap:8px}.auditActions a,.auditActions button,.reportButton{border:0;background:#111;color:#fff;border-radius:999px;padding:10px 13px;text-decoration:none;font-size:10px;font-weight:800;white-space:nowrap}.auditActions button{background:#f0efe9;color:#222}.analysisHeader{display:flex;justify-content:space-between;gap:20px;border-bottom:1px solid #deddd5;padding-bottom:18px;margin-bottom:15px}.analysisHeader h2{font-size:34px;letter-spacing:-.045em;margin:5px 0}.analysisHeader p{margin:0;color:#77766e;font-size:12px}.analysisActions{display:flex;align-items:center;gap:18px}.reportButton{cursor:pointer}.reportButton:disabled{opacity:.55}.analysisStats{text-align:right}.analysisStats strong{display:block;font-size:34px}.analysisStats span,.analysisStats em{display:block;font-size:10px;color:#77766e;font-style:normal}.analysisStats em{margin-top:5px;color:#a33}.analysisLayout{display:grid;grid-template-columns:320px 1fr;gap:14px;align-items:start}.findingIndex{border:1px solid #deddd5;background:#fff;border-radius:15px;padding:10px;position:sticky;top:94px}.indexTitle{font-size:10px;font-weight:850;letter-spacing:.08em;text-transform:uppercase;color:#77766e;padding:7px 9px 10px}.findingIndex button{width:100%;display:flex;gap:9px;align-items:flex-start;text-align:left;border:0;background:transparent;border-radius:10px;padding:10px}.findingIndex button.active{background:#f5f4ee}.findingIndex button:hover{background:#f8f7f2}.findingIndex button strong{display:block;font-size:11px;line-height:1.3}.findingIndex button small{display:block;color:#8a8880;font-size:9px;margin-top:4px;text-transform:capitalize}.indexDot{width:25px;height:25px;border-radius:50%;display:grid;place-items:center;flex:0 0 auto;color:#fff;font-size:9px;font-weight:850}.pink{background:#ec268f}.purple{background:#6d20c9}.orange{background:#f06d13}.blue{background:#1773e6}.green{background:#12a96d}.yellow{background:#f0ae00}.violet{background:#7a24c6}.teal{background:#09a59b}.rose{background:#e92a78}.cyan{background:#1688df}.analysisDetail{background:#fff;border:1px solid #deddd5;border-radius:15px;padding:25px}.detailTop{display:flex;gap:8px;align-items:center;color:#777;font-size:11px}.severityBadge{border-radius:999px;padding:6px 9px;color:#fff;font-size:9px;text-transform:uppercase;font-weight:850}.severityBadge.high{background:#a7352b}.severityBadge.medium{background:#b97917}.severityBadge.low{background:#50719b}.analysisDetail>h2{font-size:28px;letter-spacing:-.04em;margin:10px 0}.detailDescription{font-size:14px;line-height:1.5;color:#4e4d47}.storedEvidence{display:flex;gap:12px;align-items:center;background:#f7f6f1;border:1px solid #e2e0d8;border-radius:12px;padding:13px}.storedEvidence strong,.storedEvidence span{display:block}.storedEvidence strong{font-size:12px}.storedEvidence span{font-size:10px;color:#777;margin-top:4px}.detailGrid{display:grid;grid-template-columns:1fr 1fr;gap:12px;margin-top:12px}.detailGrid section{border:1px solid #e2e0d8;border-radius:12px;padding:16px}.detailGrid h3{font-size:16px;margin:6px 0 10px}.detailGrid p,.detailGrid li{font-size:11px;line-height:1.5;color:#555}.detailRecommendation{margin-top:12px;background:#111;color:#fff;border-radius:12px;padding:14px;font-size:12px;line-height:1.45}.storedScreenshot{margin-top:20px}.storedScreenshotHead{display:flex;justify-content:space-between;align-items:end;margin-bottom:10px}.storedScreenshotHead h3{font-size:17px;margin:4px 0}.storedScreenshotHead small{font-size:10px;color:#777}.storedCanvas{position:relative;border:1px solid #dddcd4;border-radius:13px;overflow:auto;max-height:720px;background:#f2f1eb}.storedCanvas img{display:block;width:100%;height:auto}.storedMarker{position:absolute;transform:translate(-50%,-50%);width:28px;height:28px;border:2px solid #fff;border-radius:50%;color:#fff;font-size:9px;font-weight:900;box-shadow:0 3px 9px rgba(0,0,0,.25)}.storedMarker.selected{width:36px;height:36px;border-width:3px;z-index:3}@media(max-width:850px){.kbNav{padding:0 20px}.kbHero{padding-top:45px;display:block}.kbStat{margin-top:25px;border-left:0;border-top:1px solid #deddd5;padding:16px 0 0}.clientGrid{grid-template-columns:1fr}.auditRow{grid-template-columns:1fr}.auditActions{justify-content:flex-start;padding-left:100px}.analysisHeader{display:block}.analysisActions{margin-top:15px;justify-content:space-between}.analysisLayout,.detailGrid{grid-template-columns:1fr}.findingIndex{position:static}.analysisDetail{padding:18px}}
    `}</style>
  </main>;
}
